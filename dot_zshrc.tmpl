[[ ! -r '/Users/raedm/.opam/opam-init/init.zsh' ]] || source '/Users/raedm/.opam/opam-init/init.zsh' >/dev/null 2>/dev/null
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
eval "$(zoxide init zsh)"
eval $(thefuck --alias)

export GPG_TTY=$(tty)
gpgconf --launch gpg-agent
{{.zshrc_gpg}}

for file in ~/scripts/rc/*.zsh; do
  source "$file"
done
